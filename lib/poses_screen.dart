
import 'package:flutter/material.dart';

class PosesScreen extends StatelessWidget {
  const PosesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Poses Screen'),
    );
  }
}
